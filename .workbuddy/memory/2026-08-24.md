# 2026-08-24

## 网站设计架构梳理（个人网站 Jekyll 项目）
- 基底：academicpages（Minimal Mistakes 分支）Jekyll 主题，单页 `_pages/about.md` 为首页，按 anchor 切章节。
- 核心创意：Reality/Dream 双世界主题，由 `portfolio-motion.js` 的 world-switcher 切换，状态存 sessionStorage + URL `?mode=dream`，靠 `html[data-world]` + CSS 变量 `--world-*` 驱动。
- 样式分层：`assets/css/main.scss`（基础/配色/卡片/Hero）、`_sass/_job-profile.scss`（双主题、科研猫、论文横向画廊、章节 dock）。
- 动效：particles.js（粒子背景）、GSAP+ScrollTrigger（首屏进场、滚动进度、论文 pin 画廊）；科研猫交互在 portfolio-motion.js。
- 关键文件：改配色看 main.scss / _job-profile.scss；改内容看 about.md；改元信息看 _config.yml。
- 已读文件：_config.yml、_layouts/default.html、_pages/about.md、assets/css/main.scss、_includes/scripts.html、_includes/head/custom.html、assets/js/portfolio-motion.js、_sass/_job-profile.scss。

## 电子宠物「科研猫」重构（程序化 Canvas）
- 新建 `assets/js/lab-cat-pet.js`：纯 canvas 2D 程序化绘制猫（头/耳/眼/瞳/鼻/须/爪/尾），含呼吸、眨眼、摆尾、耳朵抽动、瞳孔跟随鼠标、弹簧蹦跳、闲置 wander；情绪状态机 idle/curious/happy/sleepy/sleeping；爱心/星光/Z 粒子；localStorage 亲密度（键 `lct-pet-affection`，5 颗心显示）；prefers-reduced-motion 降级为静态；DPR 适配；页面隐藏暂停。reality/dream 双调色板按 `html[data-world]` 实时切换。
- 接入：`portfolio-motion.js` 的 `buildMotionChrome` 把原来的 `<img class="hero-lab-cat">` 换成 `<canvas>`，删除旧 react()/is-curious/is-idle-look 逻辑，调用 `window.LabCatPet.mount(canvas,{stage:catStage})`；保留 `.hero-cat-stage`/`.hero-cat-motion` 以兼容 GSAP 视差与世界切换。
- `scripts.html` 在 portfolio-motion.js 前加 `<script src="assets/js/lab-cat-pet.js?v=20260824a">`。
- `_job-profile.scss` 加 `canvas.hero-lab-cat{animation:none!important}` 关闭 CSS hop，避免与 canvas 内动画冲突。
- 验证：两个 JS 过 `node --check`；`_job-profile.scss` 单独 sass 编译通过（47KB）。完整 jekyll 构建在本机跑不了（ruby sass 3.7 与主题 vendor/breakpoint 不兼容，且未装 bundler）——属环境限制，非本次改动问题；本地用 `bundle exec jekyll serve` 即可预览。

## 电子宠物隔离预览页（12:38）
- 新增 `preview/pet-preview.html`：加载真实 `assets/js/lab-cat-pet.js` + 复制自 _job-profile.scss 的 .hero-cat-stage/canvas CSS，带「现实/梦境 皮肤切换」与「摸摸它」按钮，便于无 jekyll 环境直接交互验证宠物模块。
- 用 `python -m http.server 8765`（后台 task 45o896）在项目根目录起静态服务器，预览页以相对路径加载 /assets/js/lab-cat-pet.js；已 curl 校验两资源 200。
- 说明：预览仅验证宠物本身；整站联调（含 world-switch 过场）需用户本地 `bundle exec jekyll serve`。

## 双生作品集 TWIN PORTFOLIO 重构（12:47 起）
- 需求：同一套 HTML/文字，REALITY（冷白编辑档案/招聘向，动效3/10）与 DREAM（深黑蓝生物宇宙/记忆向，动效8/10）两套皮肤；右上角切换器 + 800ms 瞳孔过场（猫闭眼→瞳孔光环扩张→换世界→睁眼）；?mode=dream 直开；移动端禁滚动劫持、论文原生横滑、梦境粒子移动端减半；prefers-reduced-motion 降级；不改个人/论文文字。
- 改动文件：
  - `_pages/about.md`：重写为双封面 + 各 section 包 `<section class="world-section" data-world-section id data-archive data-tags markdown="1">`，文字逐字保留；封面含现实/梦境两块（各带 hero-cat-motion canvas）。
  - `_sass/_twin.scss`（新）：两套世界变量 + 封面 + 切换器 + 瞳孔过场 + 各模块双模式差异（教育星体/项目核×星/实习扫描舱/论文横向画廊+雾中聚焦+猫翻页/技能矩阵vs控制台/荣誉星群/生活记忆窗/联系深空波纹）+ 响应式 + reduced-motion。
  - `_sass/_job-profile.scss`：停用（仅留注释），main.scss 改 `@import "twin"`。
  - `assets/js/portfolio-motion.js`：重写为世界控制器（initWorld 读 ?mode/sessionStorage 默认 reality、buildSwitcher、transitionTo 瞳孔过场、buildDreamField 生物荧光粒子 canvas、buildPublications 横向画廊+雾聚焦+猫翻页、buildSectionDock 右侧导航、mountCoverCats）。
  - `assets/js/lab-cat-pet.js`：扩展梦境猫（金色发光眼、半透明身体 bodyAlpha、尾巴星点、移动残影 trail、幅度更大、pet:sleep 闭眼事件）。
  - `_includes/scripts.html`：去掉 gsap/ScrollTrigger/particles，改为 lab-cat-pet + portfolio-motion（顺序重要）。
- 验证：node --check 两 JS 通过；ruby sass 单独编译 `_twin.scss` 通过（22KB）；起静态预览 `preview/twin-preview.html` + `preview/twin.css`（真实编译 CSS）+ 真实 JS，127.0.0.1:8765 可访问。
- 风险：本机无法跑 jekyll，about.md 的 `<section markdown="1">` 嵌套 kramdown 未被真实构建验证；需用户本地 `bundle exec jekyll serve` 确认。沉浸式各模块（星体/核/扫描/星群）为 CSS 实现，非 canvas/3D。
